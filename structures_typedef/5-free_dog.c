#include "dog.h"                                                                                                                                
#include <stdio.h> 
#include <stdlib.h>                                                                                                                             
#include <string.h>

void (dog_t *d);

}
if (dog_t == NULL)
	free (dog_t);

if (*d == NULL)
	free (*d);
{
