#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main
*Return: 0 Always (succès)
*/

int main(void)
{
	int CH;

for (CH = 0; CH <= 9; CH++)

	if (CH < 9)
	putchar(',');
putchar(CH);
putchar('\n');

return (0);

}
