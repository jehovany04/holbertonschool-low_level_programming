#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - Print if is positive or negative
*Return: 0 Always (succès)
*/
int main(void)

{
	char A;

for (A = 'a'; A <= 'z'; A++, "\n", "q, e")
if (A != 'e' && A != 'q')
putchar (A);
putchar ('\n');

return (0);

}
