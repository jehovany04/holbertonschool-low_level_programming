#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - Print if is positive or negative
*Return: 0 Always (succès)
*/

int main(void)
{
	char L;

	for (L = 'a'; L <= 'z'; L++)
	putchar (L);
	putchar ('\n');

	return (0);
}
