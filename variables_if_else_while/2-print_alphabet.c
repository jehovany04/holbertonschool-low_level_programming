#include <stdlib.h>
#include <time.h>

/**
*main - Print if is positive or negative
*Return: 0 Always (succès)
*/

int main(void)
{
		int L;
srand(time(0));
n = rand() - RAND_MAX / 2;

	for (L = 'a'; L <= 'z'; L++)
	putchar("L\n", L)

	return (0);
}
