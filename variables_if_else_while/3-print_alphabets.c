#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - Print if is positive or negative
*Return: 0 Always (succès)
*/
int main(void)

{
	char L, l;

	for (L = 'a'; L <= 'z'; L++)
	for (l = 'a'; l <= 'z'; l++)

putchar (L);
putchar ('\n');
putchar (l);
putchar ('\n');

return (0);
}
