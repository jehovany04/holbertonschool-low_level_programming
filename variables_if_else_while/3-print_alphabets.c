#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Print if is positive or negative
*
* Return: 0 Always (succès)
*/
int main(void)
{
	char L;
	char l;

	for (l = 'a'; l <= 'z'; l++ && L = 'A'; L <= 'Z'; L++)
putchar (L),
putchar (l),
putchar ('\n');

return (0);
}
